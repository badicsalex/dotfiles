ff(){
    find . -name "*$1*"
}

alias p=python3
alias v='nvim'
alias vi='nvim'
alias vim='nvim'
unalias ag 2>/dev/null || true

alias ipal='ip a l'
alias wipal='watch ip a l'
alias p=python3

alias grbom='git rebase origin/master -i --autostash --autosquash'

if [ -f ~/.aliases.local ]; then source ~/.aliases.local; fi
